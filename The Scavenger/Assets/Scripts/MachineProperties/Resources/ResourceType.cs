namespace Scavenger
{
    /// <summary>
    /// Represents a type of resource that buffers and cables work with.
    /// </summary>
    public enum ResourceType
    {
        Energy,
        Item,
        Fluid,
        Data
    }
}
